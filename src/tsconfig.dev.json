{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		// Disable removeComments to speed up the build time
		"removeComments": false,
		// Disable some checks to not fail on not finished code
		"noUnusedLocals": false,
		"allowUnreachableCode": true
	}
}
